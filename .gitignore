# Python
__pycache__/
*.pyc
.ruff_cache/

# Local config / secrets — never commit
.env
.env.*
config.local.sh
*.key
*.pem

# Runtime state produced by the hooks (don't ship your own session data)
state.json
session-log.json
friction.jsonl
LEARNINGS.md
backup/
.DS_Store
